server:
  port: 8085
spring:
  application:
    name: eureka‐server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8085/eureka
